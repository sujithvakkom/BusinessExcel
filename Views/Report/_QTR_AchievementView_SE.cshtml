
@using BusinessExcel.Models
@using BusinessExcel.Controllers
@using BootstrapHtmlHelper
@using BusinessExcel.Providers.ProviderContext
@using BusinessExcel.Providers.ProviderContext.Entities
@using BusinessExcel.Controllers.JSON

@model TargetAchievementQTRView




@*<div class="row">
    <div class="col-md-4">*@

@*<div class="box box-primary" style="margin-top:3%" id="@Model.target_id">
    <div class="box-body">*@

<div class="row">
    <div class="col-md-12">


        <div id="target-achievement-view" class="table-responsive">
            @{

                using (var db = new SalesManageDataContext())
                {
                    WebGridColumn col = new WebGridColumn()
                    {

                    };

                    int count = 0;
                    IQueryable<TargetAchievementQTRView> data = db.TargetAchievementQRTViewPaging(pageNumber: ViewBag.UserTargetViewSEPage,
                                pageSize: 20,
                                sort: ViewBag.UserTargetViewSESort,
                                sortdir: ViewBag.UserTargetViewSEDir,
                                count: out count, Filters: Model
                                );

                    var grid = new WebGrid(ajaxUpdateContainerId: "target-achievement-view",
                                                    ajaxUpdateCallback: "targetachievementviewcallBack",
                                                    rowsPerPage: 20
                                                    );
                    grid.Bind(source: data, autoSortAndPage: false, rowCount: count);
                    @grid.GetHtml(

                 tableStyle: "webgrid",
                 headerStyle: "gridheader",

                              htmlAttributes: new { @id = "Tsheet" + Model.target_id },
                              columns: new[] {


                           grid.Column(
                           columnName: "Category",
                           header: "category",
                           format: null,
                           style: "category",
                           canSort: false),
                           grid.Column(

                           columnName: "Target_amt",
                           header: "Target",
                           format: (item)=>@Html.AdminLTEParagraph((object)@item["Target_amt"], new Dictionary<string, object> { { "class", "numaric" } }),
                           style: "Target_amt numaric", canSort: false),
                           grid.Column(
                           columnName: "Achieved_Amt",
                           header: "Achieved",
                           format: (item)=>@Html.AdminLTEParagraph((object)@item["Achieved_Amt"], new Dictionary<string, object> { { "class", "numaric" } }),
                           style: "Achieved_Amt numaric", canSort: false),
                              grid.Column(
                           columnName: "Achieved_Amt",
                           header: "Achieved%",
                           format: (item)=>@Html.AdminLTEParagraph((object)@item["Achieved_Amt"], new Dictionary<string, object> { { "class", "numaric" } }),
                           style: "Achieved_Amt numaric", canSort: false),

                              }

                )
                }



            }

        </div>
    </div>
</div>
<div class="row">
    <div class="col-md-3">

        <table class="table table-bordered" style="background-color:#bbbab8;margin:4px">

            <tbody>
                <tr>

                    <td class="lblcol">Base Incentive</td>
                    <td>1500</td>
                    <td></td>
                </tr>
                <tr>

                    <td class="lblcol">Qtr Sell In Parameters</td>
                    <td colspan="2"></td>
                </tr>
                <tr>

                    <td class="lblcol">Achived % -Qrtr</td>
                    <td>0.0%</td>
                    <td>0.0</td>
                </tr>
                <tr>

                    <td class="lblcol">Lines Achv</td>
                    <td>0</td>
                    <td>0.0</td>
                </tr>
                <tr>

                    <td class="lblcol">Bonus Lines</td>
                    <td>0.0%</td>
                    <td>0.0</td>
                </tr>
                <tr>

                    <td class="lblcol">Total Q2, 2018 Incentive:</td>
                    <td></td>
                    <td>0.0</td>
                </tr>
                <tr>

                    <td class="lblcol"> Received (Mnthly):</td>
                    <td colspan="2"></td>

                </tr>
                <tr>

                    <td class="lblcol"> Balance for  Q2, 2018 :</td>
                    <td colspan="2"></td>

                </tr>
            </tbody>
        </table>
    </div>
    <div class="col-md-3 qtr" id="QMNT1">
        @Html.Action(
                                                                    BusinessExcel.Controllers.ReportController.USERTARGET_ACHIEVEMENT_VIEW_SE,
                                                                    new TargetAchievementView { Month = Model.UserTargets[0].Month, user_id = Model.user_id, start_date = Model.UserTargets[0].start_date, target_id = Model.UserTargets[0].target_id })




    </div>
    <div class="col-md-3 qtr" id="QMNT2">

        @Html.Action(
                                                                    BusinessExcel.Controllers.ReportController.USERTARGET_ACHIEVEMENT_VIEW_SE,
                                                                    new TargetAchievementView { Month = Model.UserTargets[0].Month, user_id = Model.user_id, start_date = Model.UserTargets[0].start_date, target_id = Model.UserTargets[0].target_id })


    </div>
    <div class="col-md-3 qtr" id="QMNT3">

        @Html.Action(
                                                                    BusinessExcel.Controllers.ReportController.USERTARGET_ACHIEVEMENT_VIEW_SE,
                                                                    new TargetAchievementView { Month = Model.UserTargets[0].Month, user_id = Model.user_id, start_date = Model.UserTargets[0].start_date, target_id = Model.UserTargets[0].target_id })


    </div>
</div>



<style>
   
    .lblcol{
        width:50%;
    }

    .Entered_Amt {
        width: 15%;
    }

    .webgrid {
        margin: 4px;
        border-collapse: collapse;
        width: 80%;
    }

    .gridheader {
        padding: 6px 5px;
        text-align: center;
        background-color: #e8eef4;
        /*border-bottom: 2px solid #3966A2;*/
        height: 40px;
        font-weight: bold;
        border-top: 2px solid #D6E8FF;
        border-left: 2px solid #D6E8FF;
        border-right: 2px solid #D6E8FF;
    }

    .webgrid th, .webgrid td {
        border: 1px solid #C0C0C0;
        padding: 5px;
    }
    .qtr
    {
        margin-top:2%;
    }
</style>
